<Page
    x:Class="ASDAppTemplate.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ASDAppTemplate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer x:Name="DisplayScrollViewer">
        <StackPanel>
            <TextBlock/>
            <Grid x:Name="DisplayGrid">
            <Canvas x:Name="DisplayCanvas">
                
            </Canvas>
            </Grid>
            <StackPanel>
                <Button x:Name="AButton">
                    <Button.Content>
                        <Canvas>
                            <TextBlock x:Name="AButtonText"/>
                            <TextBlock x:Name="AButtonSymbol"/>
                            <TextBlock x:Name="AButtonHotkey"/>
                        </Canvas>
                    </Button.Content>
                </Button>
                <Button x:Name="SButton">
                    <Button.Content>
                        <Canvas>
                            <TextBlock x:Name="SButtonText"/>
                            <TextBlock x:Name="SButtonSymbol"/>
                            <TextBlock x:Name="SButtonHotkey"/>
                        </Canvas>
                    </Button.Content>
                </Button>
                <Button x:Name="DButton">
                    <Button.Content>
                        <Canvas>
                            <TextBlock x:Name="DButtonText"/>
                            <TextBlock x:Name="DButtonSymbol"/>
                            <TextBlock x:Name="DButtonHotkey"/>
                        </Canvas>
                    </Button.Content>
                </Button>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
